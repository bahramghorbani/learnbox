# LB-DS-071 — Web offline and failure-recovery accessibility

- Branch: `fix/web-offline-error-recovery-a11y`
- Base commit: `b49f325c0fa328a90b7e1da0f53e0e509d69cc5a` (`origin/main`, PR #281 merge)
- Head commit: `d6541a3` (implementation commit; this handoff/report update is the branch head that follows it).
- Draft PR: not opened; the authorizing task prohibits pushes, PRs, GitHub, provider and deployment state.
- Scope completed: test-first accessibility slice for the three Web failure surfaces inside the allowed paths. `/offline` now points `aria-labelledby` at a real `#offline-title` heading and announces the offline/reconnect state through the existing message paragraph as `role="status"`; the global error boundary hides its decorative recovery illustration from assistive technology and announces its failure message as `role="alert"`; the static `public/offline.html` fallback exposes one named `main` landmark, an `#offline-title` heading and an empty-`alt` decorative illustration; `public/sw.js` carries only a cache-version literal bump from `v8` to `v9`.
- TDD evidence: three new focused test files were added and run before any production edit — RED was `Test Files 3 failed (3)` / `Tests 8 failed | 8 passed (16)` with the genuine assertions failing (`aria-labelledby` target missing, no `role="status"`, no reconnect state change, illustration not `aria-hidden`, message not `role="alert"`, missing `aria-labelledby`/`id` in the fallback, `alt=""` missing, `[ '8' ] to deeply equal [ '9' ]`). RED was re-confirmed with the final test files by stashing the production edits (`Tests 8 failed | 8 passed (16)`), then GREEN after the minimal production edits (`Test Files 3 passed (3)` / `Tests 16 passed (16)`).
- Files changed: implementation commit `d6541a3` changed `apps/website/app/offline/page.tsx`; `apps/website/app/error.tsx`; `apps/website/public/offline.html`; `apps/website/public/sw.js`; `apps/website/test/offline-route.test.tsx`; `apps/website/test/error-state.test.tsx`; `apps/website/test/offline-fallback.test.tsx` (310 insertions, 12 deletions). This follow-up commit changes `.ai/WORK_QUEUE.md` and `.ai/worker-reports/LB-DS-071.md` only.
- Checks run: focused offline/error/fallback RED then GREEN 16/16; `pnpm --filter @learnbox/website test` 43 files / 292 tests passed; `pnpm --filter @learnbox/website typecheck` exit 0; `pnpm --filter @learnbox/website build` exit 0 with `/offline` prerendered as static content; `pnpm verify:security` reported `Validated web security and PWA boundaries.`; full `pnpm check` exit 0 (Prettier `format:check`, ESLint, recursive typecheck and package/service tests, queue, documentation-governance, security, continuity and all other verify scripts); Prettier on all seven changed files reported `All matched files use Prettier code style!`; `git diff --check` clean. The website `test`/`typecheck` scripts required building `@learnbox/learning-engine`, `@learnbox/billing-core` and `@learnbox/api` first in this fresh worktree; that was done locally with `pnpm install --frozen-lockfile --offline` and has no external effect.
- Checks unavailable: independent exact-head review and the seven terminal GitHub/Vercel contexts were not run because this task prohibits push, PR and external state; they remain the merge gate. No browser, axe or assistive-technology session was run, so no such coverage is claimed.
- Reduced motion and RTL: no CSS, font or animation source was touched (`app/globals.css` untouched), so the existing `prefers-reduced-motion` blocks and the Persian RTL layout are unchanged rather than re-verified by a rendered probe.
- Remaining work: independent exact-head review, PR creation and CI (not authorized here), then queue/product status reconciliation on rebase.
- Risks: the new reconnect copy `دوباره آنلاین شدی؛ برای ادامه دوباره تلاش کن.` claims nothing about sync or recovery of learner events; the two pre-existing `.offline-message`/`.offline-reassurance` lines were left byte-identical because the same device-local reassurance wording lives in the out-of-scope global `NetworkStatus` component and changing it would be a product-copy decision outside the allowed paths; the `v9` cache version invalidates the previous cached shell on the next activation, which is the intended delivery mechanism and does not alter caching logic or credential boundaries.
- Secrets or production changes: none. No provider, credential, flag, Preview/Production, auth/session, API, database, migration, media, content or release state was touched; no push, PR or GitHub call was made.
- Bobo canonical status: unchanged; the existing `recovery-v2` asset is only referenced, and the error-boundary illustration is now marked decorative without changing the assistant's canonical appearance.
