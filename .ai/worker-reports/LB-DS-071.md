# LB-DS-071 — Web offline and failure-recovery accessibility

- Branch: `fix/web-offline-error-recovery-a11y`
- Base commit: `b49f325c0fa328a90b7e1da0f53e0e509d69cc5a` (`origin/main`, PR #281 merge)
- Head commit: read live from Draft PR #282 with `gh pr view 282 --json headRefOid` before readiness or merge; `d6541a3` is the original implementation commit, `3034715` is the blocked reviewed head, and `fde9272` is the independently accepted exact code head.
- Draft PR: #282 — https://github.com/bahramghorbani/learnbox/pull/282
- Scope completed: test-first accessibility slice for the three Web failure surfaces inside the allowed paths. `/offline` now points `aria-labelledby` at a real `#offline-title` heading, reads `navigator.onLine` after hydration, keeps its visible heading/message consistent, leaves the global `NetworkStatus` as the sole non-empty offline announcement and announces reconnect through an initially empty screen-reader-only `role="status"`; the global error boundary hides its decorative recovery illustration from assistive technology and announces its failure message as `role="alert"`; the static `public/offline.html` fallback exposes one named `main` landmark, an `#offline-title` heading and an empty-`alt` decorative illustration; `public/sw.js` carries only a cache-version literal bump from `v8` to `v9`.
- TDD evidence: three new focused test files were added and run before any production edit — RED was `Test Files 3 failed (3)` / `Tests 8 failed | 8 passed (16)` with the genuine assertions failing (`aria-labelledby` target missing, no `role="status"`, no reconnect state change, illustration not `aria-hidden`, message not `role="alert"`, missing `aria-labelledby`/`id` in the fallback, `alt=""` missing, `[ '8' ] to deeply equal [ '9' ]`). RED was re-confirmed with the final test files by stashing the production edits (`Tests 8 failed | 8 passed (16)`), then GREEN after the minimal production edits (`Test Files 3 passed (3)` / `Tests 16 passed (16)`). Independent review at `3034715` then found an already-online mount and reconnect heading contradiction; the remediation tests failed genuinely 3/6 before the fix and pass 7/7 after it, including a composed `NetworkStatus` + `/offline` live-region regression.
- Files changed: `apps/website/app/offline/page.tsx`; `apps/website/app/error.tsx`; `apps/website/public/offline.html`; `apps/website/public/sw.js`; `apps/website/test/offline-route.test.tsx`; `apps/website/test/error-state.test.tsx`; `apps/website/test/offline-fallback.test.tsx`; `.ai/WORK_QUEUE.md`; `.ai/worker-reports/LB-DS-071.md`.
- Checks run: focused offline/error/fallback GREEN 18/18 after the review remediation; `pnpm --filter @learnbox/website test` 43 files / 294 tests passed; `pnpm --filter @learnbox/website typecheck` exit 0; `pnpm --filter @learnbox/website build` exit 0 with `/offline` prerendered as static content; `pnpm verify:security` reported `Validated web security and PWA boundaries.`; full `pnpm check` exit 0 (Prettier `format:check`, ESLint, recursive typecheck and package/service tests, queue, documentation-governance, security, continuity and all other verify scripts). The website `test`/`typecheck` scripts required building `@learnbox/learning-engine`, `@learnbox/billing-core` and `@learnbox/api` first in this fresh worktree; that was done locally with `pnpm install --frozen-lockfile --offline` and has no external effect.
- Checks unavailable: final independent review of the live PR head and seven terminal GitHub/Vercel contexts are pending; they remain the merge gate. Independent review at `3034715` blocked, and replacement review of exact code head `fde9272` passed after remediation. No browser, axe or assistive-technology session was run, so no such coverage is claimed.
- Reduced motion and RTL: no CSS, font or animation source was touched (`app/globals.css` untouched), so the existing `prefers-reduced-motion` blocks and the Persian RTL layout are unchanged rather than re-verified by a rendered probe.
- Remaining work: final exact-head review and seven terminal-success contexts on Draft PR #282, then merge and stable queue/product status reconciliation.
- Risks: the reconnect copy `دوباره آنلاین شدی؛ برای ادامه دوباره تلاش کن.` claims nothing about sync or recovery of learner events; the offline and reconnect headings now track the live browser state after hydration; the composed surface keeps only one non-empty connection announcement in either state without changing the out-of-scope global `NetworkStatus`; the `v9` cache version invalidates the previous cached shell on the next activation, which is the intended delivery mechanism and does not alter caching logic or credential boundaries.
- Secrets or production changes: none. No credential, flag, Preview/Production activation, auth/session, API, database, migration, media, content or release state was touched. The branch was pushed and Draft PR #282 opened for required CI; only repository CI and its automatic preview status contexts are authorized, with no manual provider or deployment mutation.
- Bobo canonical status: unchanged; the existing `recovery-v2` asset is only referenced, and the error-boundary illustration is now marked decorative without changing the assistant's canonical appearance.
