# LearnBox current work

**Scope:** only unfinished work. Stable merged facts live in `PROJECT_STATE.md`; capability truth lives in `docs/PRODUCT_STATUS.md`; release sequencing lives in `ROADMAP.md`; task authorization lives in `.ai/WORK_QUEUE.md`.

## Active work

Draft PR #289 carries LB-DS-077's semantic separation hardening and is awaiting exact-head review and CI. LB-DS-073 and LB-DS-074 merged through PRs #285–#286. The bounded owner-authorized operation then uploaded exactly 105 selected assets to the verified isolated private target; initial and complete resume runs verified every object by cache-disabled private download, exact byte count and SHA-256. The URL-free receipt remains outside Git. Nothing is attached, review-approved, seeded, activated, deployed or published. LB-DS-075 reconciled that truth through merged PR #287. LB-DS-076 prepares the deterministic 35-item, six-dimension repository-only review packet for the batched owner decision; it records no check outcome and no human decision. LB-DS-077 records the owner's returned batched review — 210/210 explicit owner-submitted `passed` checks (`ownerSubmittedOutcome`, never a bare ambiguous `outcome`, with `adminOutcomeRecorded: false` on every check) and 35 `approve` decisions with no return or reject — as normalized repository evidence bound to source merge `f0f413b`. It states that the owner accepted one self-contained offline 35-card review artifact's content and card flow (`app_flow` keeps the packet's `pending_unproven` / `unproven_at_release_level`), not Production or runtime behavior; keeps both prior transcription exceptions as accepted-as-presented and unpersisted; and asserts no cryptographic or session identity proof. The Admin review store, media attachment, seed and publication stay untouched.

### Active release objective

Official Web/PWA v1.0 is targeted for **2026-10-12**. It includes 35/35 human-approved starter items, server-authoritative learning with lossless reconnect, release-critical account/privacy/support and exercised operations. Native Android/Cafe Bazaar, every payment path and premium packs are v1.1+.

## Deadline-critical owner gates

1. **Completed Sep 12:** create an isolated private-media target inside the current plan; the owner attests its boundary, which authorized no upload.
2. **Completed Sep 14, bounded:** guarded private upload plus integrity verification for exactly 105 selected assets completed after reviewed tooling; attachment remains separately prohibited.
3. **Completed Sep 14, repository evidence only:** the owner returned one batched review of all 35 starter items across the six auditable dimensions — 210/210 owner-submitted checks `passed`, 35 `approve`, no `reject` or `return_for_revision` — recorded by LB-DS-077 against source merge `f0f413b`. Those outcomes are owner intent, never Admin review outcomes: every check says `adminOutcomeRecorded: false` and the Admin store stays at 0 recorded / 210 pending. The decisions exist only as repository evidence: no Admin/database review row, media attachment, seed or publication occurred.
4. By 2026-09-22: after separate owner authorization for Admin persistence and attachment, execute that recorded decision through the reviewed Admin path and attach the approved private media, which is what actually reaches 35/35 release approval.
5. By 2026-09-26: approve starter seed and staging activation after 35/35 release approval.
6. By 2026-10-11: approve phased public Web/PWA rollout after S4 exit evidence.

A missed owner gate moves the 2026-10-12 target one-for-one unless a safe, explicit scope decision removes that dependency. It never authorizes bypassing security, review or release controls.

## Immediate execution order

1. **Completed Sep 14:** LB-DS-074 delivered byte-identical JPEG renames, regenerated contracts, route/mobile corrections, full checks, independent exact-head review and seven terminal-success CI contexts through merged PR #286.
2. **Completed Sep 14:** PR #286 merged corrected JPEG source truth after exact-head review and seven green contexts.
3. **Completed Sep 14:** exactly 105 selected assets were uploaded privately to the verified isolated target; initial and complete resume runs verified every object by cache-disabled private download, exact byte count and SHA-256. The URL-free receipt remains outside Git; nothing is attached.
4. **Prepared Sep 14:** LB-DS-076 generated and validated the deterministic 35-item six-dimension review packet with source-local evidence, selected-image alt text, rollback/version linkage and explicit known exceptions; its hardening commit labels the image alt text as derived and unreviewed, states `databaseRowsVerified: false` for every item/version with repository/migration linkage rather than live database truth, marks `repositoryLocalMedia` true only for the 20 original items whose media is present in this repository, and states the final-15 visual/audio evidence as batch-aggregate rather than per-item proof. The owner subsequently completed the artifact review recorded by LB-DS-077; that repository evidence is not Admin persistence, attached media or release approval.
5. **Completed Sep 14:** the owner returned the batched six-dimension review of all 35 items; LB-DS-077 records it as the deterministic `content/packs/learnbox-start/validation/start-a1-35-owner-review-decisions.json` with its validator and focused tests wired into `pnpm check`. The record separates owner-submitted evidence from Admin/release state (`reviewPresentation`, `adminOutcomeRecorded: false`, preserved prior exceptions, owner-asserted reviewer role) and authorizes no persistence, attachment, seed, activation or publication.
6. After separate owner authorization for Admin persistence and attachment, execute the recorded 35 `approve` decisions through the reviewed path by 2026-09-22; only that path can make an item release-approved.
7. Keep attachment, seed, activation and learner exposure downstream of their separate gates and 35/35 release approval.
8. Admit no Android, payment, premium or generalized AI-factory work into v1.0.
