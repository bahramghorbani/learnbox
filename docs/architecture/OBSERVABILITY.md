# Observability

Structured logs omit secrets and sensitive content. Track request errors, OTP abuse, queue latency, sync conflicts, media failures, migrations, and client crashes. Define production alert thresholds before launch.
