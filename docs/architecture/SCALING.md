# Scaling

Start managed/provider-neutral. Scale stateless API and workers horizontally, cache read-heavy content, use object storage/CDN for media, and move expensive content generation off the request path.
