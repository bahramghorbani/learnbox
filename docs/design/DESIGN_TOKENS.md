# Design tokens

Foundation semantic CSS tokens live in `packages/design-system/src/tokens.css`. Adopt a production Persian typeface only after licensing is recorded. Verify contrast and dynamic text scaling.
