# User journeys

Register with OTP → brief goal-led onboarding → Today recommendation → active recall card → grade → local event stored → sync when online → calm progress feedback. Recovery mode reduces a missed-days backlog.
