# Backup and restore

Before production, automate encrypted PostgreSQL backups, object-storage versioning, restore drills, and retention policies. Never test restores with real user data outside controlled infrastructure.
