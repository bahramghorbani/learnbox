# ثبت شواهد آلفای بسته

این فرم پیش از دعوت هر شرکت‌کننده تکمیل می‌شود. خانه‌های خالی به‌معنای آماده‌نبودن هستند؛ این فایل جایگزین تأیید مالک نیست.

## مشخصات نوبت

- نسخه / شناسهٔ ثبت Git: `e09d0e7`
- تاریخ و مسئول بررسی: `۲۰۲۶-۰۷-۲۷ / Codex و مالک پروژه (تأیید در گفت‌وگو)`
- نسخهٔ بازگشتِ تأییدشده: `aa8ca13`
- محیط: `پیش‌نمایشِ تأییدشده`

## کنترل‌های خودکار

- [x] `pnpm check` موفق است.
- [x] `pnpm build` موفق است.
- [x] `node scripts/validate-migrations.mjs` موفق است.
- [x] `pnpm audit --prod --audit-level=high` بدون آسیب‌پذیری شناخته‌شده است.
- [x] `pnpm verify:alpha` موفق است و `config/closed-alpha.json` همچنان خاموش است.
- [x] وضعیت CI همین ثبت در GitHub سبز است.

## آزمون دستی مسیر اصلی

برای هر مورد، نسخهٔ مرورگر/دستگاه و نتیجه را ثبت کن؛ شمارهٔ تلفن، کد ورود یا دادهٔ شخصی را وارد این فرم نکن.

| بررسی                                          | دستگاه یا مرورگر                 | نتیجه  | شناسهٔ مسئله، اگر وجود دارد |
| ---------------------------------------------- | -------------------------------- | ------ | --------------------------- |
| نمایش صفحهٔ امروز                              | `مرورگر دسکتاپ؛ جزئیات ثبت‌نشده` | `موفق` | `ندارد`                     |
| ورود آزمایشی → انتخاب هدف → Today              | `گوشی واقعی؛ جزئیات ثبت‌نشده`    | `موفق` | `ندارد`                     |
| شروع مرور → برگرداندن کارت → ثبت پاسخ          | `گوشی واقعی؛ جزئیات ثبت‌نشده`    | `موفق` | `ندارد`                     |
| بازگشت به Today و دیدن وضعیت جلسه              | `آیفون / Safari`                 | `موفق` | `پیام پایان جلسه نمایش شد`  |
| نصب PWA                                        | `آیفون / Safari`                 | `موفق` | `ندارد`                     |
| باز کردن صفحهٔ امن آفلاین پس از قطع شبکه       | `آیفون / Safari`                 | `موفق` | `شفافیت بوبو: کارِ برگشتی`  |
| خوانایی RTL، اندازهٔ متن و کنترل‌های صفحه‌خوان | `آیفون / Safari`                 | `موفق` | `ندارد`                     |

## ایمنی و دامنه

- [ ] فهرست مجاز شرکت‌کنندگان و کانال دعوت توسط مالک تأیید شده است.
- [x] متن رضایت آلفا بدون تغییر ناخواسته آماده است.
- [x] پرداخت، اعلان واقعی و پیامک واقعی فعال نیستند.
- [ ] مسیر گزارش خصوصی و مسئول رسیدگی مشخص است.
- [ ] پشتیبان‌گیری و دستور بازگشت نسخه بررسی شده‌اند.
- [ ] مسئلهٔ بحرانیِ باز وجود ندارد.

## تصمیم

- [ ] برای آزمون راهنما با ۲ تا ۳ نفر آماده است.
- [ ] نیازمند اصلاح است؛ دعوت متوقف می‌ماند.
- [ ] رخداد بحرانی؛ مطابق `INCIDENT_RESPONSE.md` اقدام شده است.

یادداشت تصمیم: `________________________________________________________`

تأیید مالک برای شروع دعوت: `نام / تاریخ / روش تأیید` — این بخش فقط در زمان واقعی شروع آلفا تکمیل می‌شود.
